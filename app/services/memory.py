"""Deprecated — InMemoryOrchestratorStore has moved to infrastructure/checkpointer.py.

This module is intentionally empty.  It is kept only so that stale imports
produce a clear ImportError rather than a silent AttributeError.
"""
